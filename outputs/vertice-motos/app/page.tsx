import { ArrowDown, ArrowUpRight } from 'lucide-react';
import { site, motorcycles, copy } from '@/lib/content';
import { MotorcycleCard, WhatsAppLink } from '@/components/site';
export default function Home() {
  return <main id="conteudo">
    <section className="hero" aria-labelledby="hero-title"><img className="hero-image" src={site.heroImage} alt="Moto urbana preta em uma garagem de concreto iluminada pelo sol. Imagem ilustrativa." width="1672" height="941" fetchPriority="high" /><div className="hero-shade" /><div className="container hero-content"><p className="eyebrow">{copy.heroEyebrow}</p><h1 id="hero-title">{copy.heroTitle}<br />{copy.heroTitleSecond} <span>{copy.heroTitleAccent}</span></h1><p className="hero-description">{copy.heroDescription}</p><div className="actions"><a className="button button-primary" href="#motos">Ver motos <ArrowDown size={19} aria-hidden="true" /></a><WhatsAppLink className="button button-glass" /></div></div></section>
    <section className="section container catalog" id="motos" aria-labelledby="catalog-title"><div className="section-heading"><h2 id="catalog-title">{copy.catalogTitle}<span className="accent">.</span></h2><p>{copy.catalogDescription}</p></div><div className="motorcycle-grid">{motorcycles.map(moto => <MotorcycleCard key={moto.slug} moto={moto} />)}</div><p className="catalog-note">{copy.consultationNote}</p></section>
    <section className="section how-section" id="como-funciona" aria-labelledby="how-title"><div className="container how-layout"><div className="how-intro"><h2 id="how-title">{copy.howTitle}<span className="accent">.</span></h2><p>{copy.howDescription}</p><a className="text-link" href="#motos">Encontre sua moto <ArrowUpRight size={20} aria-hidden="true" /></a></div><ol className="steps">{copy.steps.map((step, index) => <li key={step.title}><span className="step-number" aria-hidden="true">0{index + 1}</span><div><h3>{step.title}</h3><p>{step.description}</p></div></li>)}</ol></div></section>
    <section className="section container contact-layout" id="contato" aria-labelledby="contact-title"><div className="contact-intro"><h2 id="contact-title">{copy.contactTitle}<span className="accent">.</span></h2><p>{copy.contactDescription}</p><WhatsAppLink className="button button-primary" /></div><div className="contact-info"><div><span>WhatsApp e telefone</span><a href={`tel:${site.phone}`}>{site.phoneDisplay}<ArrowUpRight size={18} aria-hidden="true" /></a></div><div><span>Onde estamos</span><address>{site.address.street}<br />{site.address.city}</address></div><div><span>Horário de atendimento</span><p>{site.hours.weekdays}<br />{site.hours.saturday}</p></div><p className="demo-note">{copy.demoContact}</p></div></section>
  </main>;
}

